namespace RevitTestFramework.Common
{
    /// <summary>
    /// Log level enum for filtering logs (duplicate of RevitTestFramework.Xunit.Contracts.LogLevel)
    /// </summary>
    public enum LogLevel
    {
        Trace = 0,
        Debug = 1,
        Info = 2,
        Warn = 3,
        Error = 4,
        Fatal = 5
    }
}
